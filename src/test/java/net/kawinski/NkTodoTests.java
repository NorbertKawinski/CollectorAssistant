package net.kawinski;

public class NkTodoTests {

    // Page title
    // i18n
    // LoginToContinue feature (continue to adminPanel.xhtml)
    // RememberMe feature (happy path/expired token)
    // Login with mail/username/usernameCaseInsensitive/wrongMailOrUser/wrongPassword
    // Logout

    // CRUD(dont forget any letter from CRUD, these are 4 operations ;))  for users, userGroups, collections, collectionCategories, etc
    //     * Czy widac element od razu po dodaniu (zdarzalo sie, ze sie nie wyswietlal do momentu odswiezenia)

    // Check if required and defaultValue works for Collection/Element attributes both when adding templates and when adding actual attributes

    // Deleting a collection should delete elements ONLY if that element is not referenced by any other collection!

    // Validation of attributes available to collection/element and 'required' flag. Check if user can add attribute they shouldn't be able to (from different category)
}
